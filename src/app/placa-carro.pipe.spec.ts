import { PlacaCarroPipe } from './placa-carro.pipe';

describe('PlacaCarroPipe', () => {
  it('create an instance', () => {
    const pipe = new PlacaCarroPipe();
    expect(pipe).toBeTruthy();
  });
});
